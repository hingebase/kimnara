[package.build.backend]
name = "pixi-build-rattler-build"
version = ">=0.3.8,<0.4.0"
channels = ["conda-forge"]

[package.build.config]
experimental = true

[package.host-dependencies]
kimnara = { path = "../kimnara" }

[package.run-dependencies]
kimnara = { path = "../kimnara" }
